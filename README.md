# Movies REST API

Простое REST API для управления каталогом фильмов на FastAPI и MySQL.

## 📌 Функционал

- `GET /movies` - список фильмов с пагинацией и фильтрацией по названию/жанру
- `GET /movies/{id}` - получить фильм по ID
- `POST /movies` - добавить новый фильм
- `PUT /movies/{id}` - обновить фильм
- `DELETE /movies/{id}` - удалить фильм

## 🛠 Технологии

- **Python 3.9+**
- **FastAPI** - веб-фреймворк
- **MySQL** - база данных
- **Pandas** - загрузка данных
- **Pydantic** - валидация данных

## 🚀 Быстрый старт

### 1. Клонировать репозиторий
```bash
git clone https://github.com/<твой-логин>/movies-api.git
cd movies-api
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Настроить базу данных
```sql
-- В MySQL выполнить:
CREATE DATABASE movies_db;
```

### 4. Настроить подключение
Замени в файлах main.py и load_data.py:
```python
host='localhost'
user='твой_логин_mysql'
password='твой_пароль_mysql'
database='movies_db'
```

### 5. Загрузить данные
```bash
python load_data.py
```

### 6. Запустить сервер
```bash
uvicorn main:app --reload
```

### 7. Открыть документацию
Перейти в браузере: http://localhost:8000/docs

## 📖 Примеры запросов
Получить фильмы с пагинацией:
```bash
GET /movies?page=1&limit=5
```

Поиск по названию:
```bash
GET /movies?title=matrix
```

Фильтрация по жанру:
```bash
GET /movies?genre=Sci-Fi
```

Добавить фильм:
```bash
POST /movies
{
    "title": "New Movie",
    "year": 2024,
    "genre": "Drama",
    "rating": 7.5
}
```

## 📁 Структура проекта
```text
movies-api/
├── main.py          # Основное приложение FastAPI
├── load_data.py     # Скрипт загрузки данных в БД
├── requirements.txt # Зависимости Python
├── movies.csv       # Пример данных
└── README.md        # Документация
```

## ⭐ Если проект был полезен, поставь звезду на GitHub!


---

### 5. Как выложить на GitHub:

1. **Создай репозиторий на GitHub** с названием `movies-api` (без README, без .gitignore)

2. **В терминале выполни:**
```bash
# Инициализируй Git
git init


# Добавь все файлы
git add .

# Сделай первый коммит
git commit -m "Initial commit: Simple Movies API with FastAPI and MySQL"

# Привяжи к GitHub репозиторию
git branch -M main
git remote add origin https://github.com/<твой-логин>/movies-api.git

# Запушь
git push -u origin main
```
